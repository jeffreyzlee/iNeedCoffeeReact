/*
import React from 'react';
import expect from 'expect';
import { shallow, mount } from 'enzyme';
import OwnerReg from '../ownerReg/OwnerReg';
const wrapper = mount(<OwnerReg/>);

describe('OwnerReg Component', () => {
  
  it('renders Owner Registration Page', () => {
    expect(wrapper.find('h1').text()).toEqual('Owner Registration');
  });

  it('renders Owner Registration Page Preferences', () => {
    expect(wrapper.find('.preferenceT').text()).toEqual(' Let Us Get Started ; Enter your three most preferred locations! ');
  });

});

*/